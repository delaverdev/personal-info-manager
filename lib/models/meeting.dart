library Meeting;
class Meeting {
  String id;
  String title;
  DateTime date;
  String description;

  Meeting({required this.id, required this.title, required this.date, required this.description});
}